# 조건에 맞는 사용자와 총 거래금액 조회하기

## 문제 요약
> 완료된 중고 거래의 총금액이 70만 원 이상인 사용자의 회원 ID, 닉네임, 총거래금액을 조회하고 총거래금액 오름차순 정렬

## 느낀점
>
- 없음

## 코드
```sql
WITH B AS (
    SELECT WRITER_ID, SUM(PRICE) AS TOTAL_SALES
    FROM USED_GOODS_BOARD
    WHERE STATUS = 'DONE'
    GROUP BY WRITER_ID
)
SELECT U.USER_ID, U.NICKNAME, B.TOTAL_SALES
FROM USED_GOODS_USER U
JOIN B ON B.WRITER_ID = U.USER_ID
WHERE B.TOTAL_SALES >= 700000
ORDER BY TOTAL_SALES
```

## 질문 & 실수 정리

### 질문했던 내용 (새로 배운 것)

없음

### 실수했던 부분

없음

## AI 코드 리뷰

### 개선할 점

1. **CTE 대신 HAVING 활용 고려**: 단일 집계 + 필터링 구조에서는 CTE 없이 HAVING으로 더 간결하게 작성 가능. CTE는 집계 결과를 여러 곳에서 재사용하거나 복잡한 다단계 쿼리에서 더 효과적.

## 수정된 코드
```sql
SELECT U.USER_ID, U.NICKNAME, SUM(B.PRICE) AS TOTAL_SALES
FROM USED_GOODS_BOARD B
JOIN USED_GOODS_USER U ON B.WRITER_ID = U.USER_ID
WHERE B.STATUS = 'DONE'
GROUP BY U.USER_ID, U.NICKNAME
HAVING SUM(B.PRICE) >= 700000
ORDER BY TOTAL_SALES
```
