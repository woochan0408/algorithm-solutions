# 🔗 연결 요소의 개수

## 느낀점
>
- 바로 풀음
- 맞긴했지만 visited 시작노드 넣는거 까먹지 말자

---

## 흐름

1. 그래프를 인접 리스트로 구성 (양방향)
2. 모든 노드를 순회하며 방문하지 않은 노드 발견 시 DFS 시작
3. DFS 시작할 때마다 연결 요소 개수 +1
4. DFS로 연결된 모든 노드 방문 처리

## 코드
```python
import sys
input = sys.stdin.readline
N, M = map(int, input().split())
graph = {}
for i in range(1, N+1):
    graph[i] = []
for _ in range(M):
    u, v = map(int, input().split())
    graph[u].append(v)
    graph[v].append(u)
visited = set()
result = 0
for i in range(1, N+1):
    if(not i in visited):
        result += 1
        stack = [i]
        while stack:
            cur = stack.pop()
            for node in graph[cur]:
                if(not node in visited):
                    visited.add(node)
                    stack.append(node)
print(result)
```

---

## 질문 & 실수 정리

### 질문했던 내용 (새로 배운 것)

| 질문 | 답변 |
|------|------|
| - | - |

### 실수했던 부분

| 틀린 것 | 올바른 것 |
|---------|-----------|
| - | - |

---

## AI 코드 리뷰

### 개선할 점

1. **시작 노드 visited 처리 누락**: 시작 노드 `i`를 visited에 추가하지 않아 인접 노드에서 다시 시작 노드로 돌아올 때 불필요한 재탐색 발생. 정답은 나오지만 비효율적.

2. **조건문 표현**: `not i in visited` → `i not in visited`가 더 파이썬스러움

3. **graph 초기화**: `defaultdict(list)` 사용하면 초기화 루프 생략 가능

4. **변수명**: `result` → `count`가 "개수"를 세는 의미에 더 적합

## 수정된 코드
```python
import sys
from collections import defaultdict

input = sys.stdin.readline
N, M = map(int, input().split())

graph = defaultdict(list)
for _ in range(M):
    u, v = map(int, input().split())
    graph[u].append(v)
    graph[v].append(u)

visited = set()
count = 0

for i in range(1, N + 1):
    if i not in visited:
        count += 1
        stack = [i]
        visited.add(i)  # 시작 노드 방문 처리
        while stack:
            cur = stack.pop()
            for node in graph[cur]:
                if node not in visited:
                    visited.add(node)
                    stack.append(node)

print(count)
```
