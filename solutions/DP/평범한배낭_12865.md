# 백준 12865 🎒 평범한 배낭 파이썬

## 느낀점
>
- 오랜만에 풀어서 로직 참고해서 풀었다.
- DP로 풀었지만 여러가지 풀이가 있다. 모두 연습해보자
(아래는 참 정리 잘 된 블로그)
- https://beyond-common-sense.tistory.com/4

## 흐름

1. N개의 물건과 최대 무게 K 입력
2. 2D DP 테이블 생성: `dp[i][j]` = i번째 물건까지 고려했을 때, 용량 j에서의 최대 가치
3. 첫 번째 물건 초기화: 용량이 물건 무게 이상이면 해당 가치 저장
4. 점화식 적용: 각 물건마다 넣거나(`dp[i-1][j-w] + v`) 안 넣거나(`dp[i-1][j]`) 중 최댓값
5. `dp[N-1][K]` 출력

## 코드

```python
import sys
input = sys.stdin.readline
N, K = map(int, input().split())
bags = []
dp = [[0]*(K+1) for _ in range(N)]
for _ in range(N):
    n, k = map(int, input().split())
    bags.append((n, k))
for i in range(K+1):
    if(i >= bags[0][0]):
        dp[0][i] = bags[0][1]
for i in range(1, N):
    for j in range(K+1):
        # 넣거나 말거나
        if(j >= bags[i][0]):
            dp[i][j] = max(dp[i-1][j], dp[i-1][j-bags[i][0]]+bags[i][1])
        else:
            dp[i][j] = dp[i-1][j]
print(dp[N-1][K])
```

## 질문 & 실수 정리

### 질문했던 내용 (새로 배운 것)

| 질문 | 답변 |
|------|------|
| (이번 대화에서 질문한 내용 없음) | - |

### 실수했던 부분

| 틀린 것 | 올바른 것 |
|---------|-----------|
| `bags` 입력 코드 누락 | 물건 정보를 입력받는 반복문 추가 |
| `bags[i]`를 무게/가치 구분 없이 사용 | `bags[i][0]`(무게), `bags[i][1]`(가치) 분리 |
| `else: dp[i-1][j]` (할당 누락) | `else: dp[i][j] = dp[i-1][j]` |
| `i=0`일 때 `dp[-1][j]` 참조 | 첫 번째 물건 별도 초기화 |
| 초기화 조건 `K >= bags[0][0]` | `i >= bags[0][0]` (현재 용량 기준) |
| `range(K)` (K 미포함) | `range(K+1)` (K 포함) |

## AI 코드 리뷰

### 개선할 점

1. **변수명 개선**: `n, k`는 입력의 `N, K`와 혼동됩니다. 무게/가치를 나타내는 `w, v`가 더 명확합니다.

2. **공간 최적화 가능**: 현재 O(N×K) 공간을 사용하지만, 1D 배열로 O(K)로 줄일 수 있습니다. 이전 행만 참조하므로 역순 순회하면 1차원으로 충분합니다.

3. **초기화 간소화**: dp 배열을 `(N+1) × (K+1)`로 만들고 0행을 기저 조건으로 두면 별도 초기화 없이 `i=1`부터 동일한 점화식을 적용할 수 있습니다.

## 수정된 코드

```python
import sys
input = sys.stdin.readline

N, K = map(int, input().split())
items = []
for _ in range(N):
    w, v = map(int, input().split())
    items.append((w, v))

# 1D DP (공간 최적화)
dp = [0] * (K + 1)

for i in range(N):
    weight, value = items[i]
    # 역순 순회: 같은 물건 중복 선택 방지
    for j in range(K, weight - 1, -1):
        dp[j] = max(dp[j], dp[j - weight] + value)

print(dp[K])
```
