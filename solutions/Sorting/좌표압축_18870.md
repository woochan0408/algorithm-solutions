# 📊 좌표 압축

## 느낀점
>
- 

---

## 흐름

1. 입력받은 배열에서 중복 제거 (set)
2. 정렬하여 각 값의 순위 결정
3. 딕셔너리에 {값: 순위} 매핑
4. 원본 배열 순서대로 순위 출력

## 코드

```python
import sys
input = sys.stdin.readline
N = int(input())
arr = list(map(int, input().split()))
setList = list(set(arr))
setList.sort()
dic = {}
for i in range(len(setList)):
    dic[setList[i]] = i
for i in arr:
    print(dic[i], end = " ")
```

---

## 질문 & 실수 정리

### 질문했던 내용 (새로 배운 것)

| 질문 | 답변 |
|------|------|
| 특별한 알고리즘이 필요한가? | 정렬 + 딕셔너리 매핑으로 해결 가능 |

### 실수했던 부분

| 틀린 것 | 올바른 것 |
|---------|-----------|
| - | - |

---

## AI 코드 리뷰

### 개선할 점

1. **변수명 개선**: `setList` → `sorted_unique`가 의미를 더 명확히 전달
2. **enumerate 활용**: 인덱스와 값을 동시에 순회할 때 더 pythonic
3. **딕셔너리 컴프리헨션**: for문 대신 한 줄로 딕셔너리 생성 가능
4. **출력 방식**: `print(*result)`로 리스트를 한 번에 출력하면 더 깔끔

## 수정된 코드

```python
import sys
input = sys.stdin.readline

N = int(input())
arr = list(map(int, input().split()))

sorted_unique = sorted(set(arr))
rank = {value: idx for idx, value in enumerate(sorted_unique)}

result = [rank[x] for x in arr]
print(*result)
```

### 개선 사항 요약

- `sorted(set(arr))`로 중복 제거 + 정렬을 한 줄에 처리
- 딕셔너리 컴프리헨션으로 매핑 생성
- 리스트 컴프리헨션으로 결과 생성 후 `print(*result)`로 출력
